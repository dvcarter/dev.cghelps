import React, { PropTypes } from 'react'

const FootOpts = () => {
    return (
            <div></div>
    )
}

FootOpts.propTypes = {
    footOpts: PropTypes.array.isRequired,
}

export default FootOpts
